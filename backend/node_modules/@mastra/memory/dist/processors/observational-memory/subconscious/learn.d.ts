import type { KnowledgeRecord, KnowledgeScope, KnowledgeStorage } from '@mastra/core/storage';
import type { ToolAction } from '@mastra/core/tools';
import type { Memory } from '../../../index.js';
import type { ObservationalMemoryModel, ReflectionCommittedContext } from '../types.js';
import type { ResolvedSubconsciousConfig } from './types.js';
type LearnerState = {
    recordedName?: string;
};
export declare function createLearnerRecordSkillTool(input: {
    store: KnowledgeStorage;
    scope: KnowledgeScope;
    pendingRecords: KnowledgeRecord[];
    parentThreadId: string;
    defaultScope: ResolvedSubconsciousConfig['defaultScope'];
    maxScope: ResolvedSubconsciousConfig['maxScope'];
    state: LearnerState;
}): ToolAction<any, any, any>;
export declare function composeReflectionAgentHandlers(handlers: Array<(context: ReflectionCommittedContext) => Promise<unknown>>): (context: ReflectionCommittedContext) => Promise<void>;
export declare function createLearnerHandler(memory: Memory, subconscious: ResolvedSubconsciousConfig, learnerMemory?: Memory, options?: {
    omModel?: ObservationalMemoryModel;
}): (context: ReflectionCommittedContext) => Promise<void>;
export {};
//# sourceMappingURL=learn.d.ts.map